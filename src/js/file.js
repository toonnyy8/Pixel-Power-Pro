import "core-js/stable"
import "regenerator-runtime/runtime"

import fs from 'fs';

// let wasm = fs.readFileSync(__dirname + '/../wasm/tfjs-backend-wasm.wasm')
// export let wasmPath = URL.createObjectURL(
//     new Blob([
//         wasm
//     ], { type: "application/wasm" })
// )
